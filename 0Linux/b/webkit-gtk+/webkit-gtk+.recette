#!/usr/bin/env bash
. /usr/share/0outils/fonctions_paquets.sh

NAMESRC=webkitgtk
<<<<<<< HEAD
VERSION=2.4.8
WGET=http://webkitgtk.org/releases/$NAMESRC-$VERSION.tar.xz
=======
VERSION=2.12.1
WGET=(
      http://webkitgtk.org/releases/$NAMESRC-$VERSION.tar.xz
      http://webkitgtk.org/releases/$NAMESRC-2.4.10.tar.xz
     )
>>>>>>> Groooooooosse mise à jour pour 0Linux iota :)
DESC="Port GTK+ du moteur de navigateur web WebKit"

telecharger_sources

# Utilisez 'ccache' ou allez vous pendre tout de suite !

# Compilaton de webkit 1 (quel enfer !) :
preparer_sources $NAMESRC-2.4.10.tar.xz
cflags

# Compilation en 2 temps (quel enfer !) :
CC=gcc CXX=g++ \
CFLAGS="${FLAGS}" CXXFLAGS="${FLAGS}" \
./configure \
	--prefix=/usr \
	--sysconfdir=/etc \
	--localstatedir=/var \
	--libdir=/usr/lib${LIBDIRSUFFIX} \
	--libexecdir=/usr/lib${LIBDIRSUFFIX}/${NAMESRC} \
	--mandir=/usr/man \
	--infodir=/usr/info \
	--docdir=/usr/doc/${NAMETGZ}-2.4.10 \
	--disable-gtk-doc \
	--disable-webkit2 \
	--enable-introspection \
	--build=${PKGARCH}-0linux-linux-gnu

make -j${JOBS} || make
fakeroot make install DESTDIR=${PKG}

CC=gcc CXX=g++ \
CFLAGS="${FLAGS}" CXXFLAGS="${FLAGS}" \
./configure \
	--prefix=/usr \
	--sysconfdir=/etc \
	--localstatedir=/var \
	--libdir=/usr/lib${LIBDIRSUFFIX} \
	--libexecdir=/usr/lib${LIBDIRSUFFIX}/${NAMESRC} \
	--mandir=/usr/man \
	--infodir=/usr/info \
	--docdir=/usr/doc/${NAMETGZ}-2.4.10 \
	--disable-gtk-doc \
	--disable-webkit2 \
	--enable-introspection \
	--with-gtk=2.0 \
	--build=${PKGARCH}-0linux-linux-gnu

make -j${JOBS} || make
fakeroot make install DESTDIR=${PKG}

# Compilaton de webkit 2 (je HAIS ce paquet !) :
preparer_sources $NAMESRC-$VERSION.tar.xz
cflags

# On supprime les tests :
rm -rf Source/ThirdParty/gtest
rm -rf Source/ThirdParty/qunit

# On doit compiler dans un répertoire dédié :
mkdir build
cd build

# Compilation :
CFLAGS="${FLAGS}" CXXFLAGS="${FLAGS}" \
cmake \
	-DCMAKE_BUILD_TYPE=Release \
	-DCMAKE_INSTALL_PREFIX=/usr \
	-DCMAKE_SKIP_RPATH=ON \
	-DLIB_INSTALL_DIR=/usr/lib${LIBDIRSUFFIX} \
	-DLIBEXEC_INSTALL_DIR=/usr/lib${LIBDIRSUFFIX}/${NAMESRC} \
	-DPORT=GTK \
	-DENABLE_GTKDOC=ON \
	..

make -j${JOBS} || make
fakeroot make install DESTDIR=${PKG}

installer_doc
creer_post_installation
stripper
empaqueter

# C'est fini.
